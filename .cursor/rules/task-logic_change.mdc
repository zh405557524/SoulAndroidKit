<!-- markdownlint-disable -->
---
description: 逻辑修改任务规则（Android）
globs:
alwaysApply: false
---

# 逻辑修改任务规则（Android）

## 修改流程
### 1. 理解现有逻辑（先搞清楚设计意图）
1. 阅读相关代码：入口（Activity/Fragment/Presenter）→ 业务实现（internal）→ 数据访问（repository/network）
2. 标注关键分支：输入校验、失败路径、并发/回调点
3. 明确“契约”：
- 哪些方法/接口是对外的（api 层）
- 哪些是内部可变的（internal/repository）

### 2. 影响范围分析（先算账再改）
1. 确定受影响文件与调用链
2. 查找依赖本逻辑的其他模块/类（避免漏改）
3. 评估是否需要更新功能测试（见 `02-功能测试.mdc`）

### 3. 修改原则（保证改动可控）
1. **保持接口一致性**
- 优先不改变对外方法签名/参数语义
- 需要变更契约时，必须同步评估所有调用方与集成点

2. **向后兼容**
- 保持原有成功路径行为尽量不变
- 失败路径行为要更清晰（更明确的错误类型/日志/返回结果）

3. **单一职责**
- 将“验证/转换/执行/反馈”拆开，减少一个方法承担多种含义

4. **失败可观测**
- 对关键路径补日志（例如 `LogUtils`）
- 避免静默失败（返回明确失败结果或抛出可捕获异常）

### 4. 代码组织建议
1. 耗时/IO 逻辑放后台执行（线程/协程/异步回调）
2. 网络请求、磁盘读写、FFmpeg 执行等与 UI 展示分离
3. 状态/结果传递要明确（成功/失败/进行中三态）

## 检查清单
- [ ] 现有功能仍可正常通过关键路径验证
- [ ] 修改点有清晰注释或日志可定位
- [ ] 失败路径不再产生未捕获异常
- [ ] 影响范围已评估，相关依赖方不会被静默破坏
- [ ] 如需要，已补齐/更新测试用例或测试页面

## 示例修改模式
- 先验证输入参数（null/空/非法）→ 再执行核心逻辑 → 最后统一处理失败/返回结果

