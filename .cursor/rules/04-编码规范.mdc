<!-- markdownlint-disable -->
---
description: Kotlin / Android 编码规范
globs:
alwaysApply: false
---

# Kotlin / Android 编码规范

## 适用范围
适用于整个工程新增与修改的 Kotlin/Java 代码（含库模块、feature 模块与 ffmpeg JNI 旁的 Java/Kotlin 层）。

## 命名约定
1. Kotlin
- 类/接口/枚举/数据类：`PascalCase`
- 函数/变量/属性：`camelCase`
- 常量：`SCREAMING_SNAKE_CASE`（如 `MAX_COUNT`）

2. 文件与资源
- Kotlin/Java 文件名使用英文小写+下划线：`snake_case`（例如 `video_player_manager.kt`）
- XML 布局与资源命名遵循项目现有规则（见 `03-ui创建规则.mdc`）

## 导入组织
建议统一导入顺序，提升可读性与 diff 友好性：
1. `java.*` / `javax.*`
2. `android.*` / `androidx.*`
3. 第三方库（`com.*`、`org.*` 等）
4. 项目内部（`com.soul.*`）

并保持：
- 同一类 import 不重复
- 避免使用过宽的通配符（如 `import com.xxx.*`）

## 空安全与健壮性
- Kotlin：避免 `!!` 强制解包
- 对外部输入（Intent extras、文件路径、网络返回、用户输入）必须做非空与边界校验
- 对可能为 `null` 的链式调用，优先使用 `?.let {}`、`?:` 返回或早退出

## 线程与异步
1. UI 线程
- 不在主线程执行耗时任务（IO、文件系统、FFmpeg 编解码、网络请求等）

2. 异步执行建议
- 若工程使用协程：优先使用结构化并发；把耗时逻辑放到后台调度器
- 若工程未使用协程：使用线程池/异步回调，并确保回到 UI 线程更新界面

## 异常处理与日志
- 捕获异常后要有“可定位信息”：记录关键上下文（模块/参数/文件名/阶段）
- 记录日志优先使用项目已有日志工具（如 `core/common` 中的 `LogUtils`）
- 不要吞掉异常：要么继续抛出、要么返回可识别的失败结果并给出用户可理解反馈

## 代码结构
- 职责单一：同一方法只做一件事（验证/转换/执行拆开）
- 提取可复用逻辑为私有方法或公共工具类（放在合适的层级：`core/*` 或 `kit/*`）
- 保持对外接口稳定：公共方法签名改动需要同步影响范围评估

## 检查清单（提交前）
- [ ] 命名/文件命名符合约定
- [ ] 未引入 `!!`（或已解释必要性）
- [ ] 耗时逻辑不在主线程执行
- [ ] 异常处理完善：日志 + 友好反馈/失败返回
- [ ] 模块依赖方向未被破坏（无循环依赖）

