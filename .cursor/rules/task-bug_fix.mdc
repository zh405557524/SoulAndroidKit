<!-- markdownlint-disable -->
---
description: Bug 修复任务规则（Android）
globs:
alwaysApply: false
---

# Bug 修复任务规则（Android）

## 目标
在不引入新问题的前提下，快速、准确地修复 bug，并补齐必要的日志/错误处理与验证步骤。

## 修复流程
### 1. 问题定位（先看“发生了什么”）
1. 复现路径：记录触发条件、输入参数、机型/系统版本、关键日志片段
2. 查证现象：崩溃/异常？功能失效？数据错误？UI 状态错乱？
3. 读相关代码：从堆栈/错误日志反向定位调用链与关键分支
4. 先做“最小信息验证”：例如确认空值/权限/文件路径/网络结果是否符合预期

### 2. 根因分析（别只修表面症状）
1. 找出导致问题的直接原因（触发条件）
2. 找出导致直接原因的根本原因（设计/边界/并发/状态管理缺陷）
3. 检查是否存在同类问题的相似路径（避免“修一个漏一片”）

### 3. 修复原则（小步、稳妥、可回滚）
1. **最小化修改**：仅修改必要代码路径
2. **保持行为一致**：除 bug 修复外，不改变其他语义
3. **边界补齐**：对 null/空数组/无权限/文件不存在/网络失败等做保护
4. **可观察性**：在关键位置补日志（使用项目已有日志封装，如 `LogUtils`）

### 4. 错误处理与用户反馈
- 崩溃类 bug：必须确保该路径不再抛出未捕获异常
- 非崩溃类 bug：必须提供清晰的失败结果（返回/状态更新）与可理解的提示
- Toast/Snackbar 使用项目已有工具封装（如 `ToastUtils`、`SnackbarUtils`），避免散落 `Toast.makeText(...)`

### 5. 测试与验证
1. 回归验证：覆盖原 bug 的关键路径
2. 边界验证：加入/验证至少一个边界条件（空、异常、权限拒绝等）
3. 性能与线程验证：确认耗时逻辑未跑主线程
4. 若存在测试页面/测试用例：补齐或更新对应功能测试（见 `02-功能测试.mdc`）

## 检查清单（提交前）
- [ ] 已复现并定位到正确代码路径
- [ ] 已解释根因，而非仅做“绕过”
- [ ] 修改范围最小化，且未破坏现有依赖方向
- [ ] 异常/失败路径有日志与友好提示（按需）
- [ ] 完成回归与边界验证

## 示例修复模式（写作模板）
- 定位：`崩溃堆栈 → 方法 → 参数 → 分支`
- 修复：`补空值/补权限/补异常捕获/修正状态`
- 验证：`原路径 + 边界路径 + 回归路径`

