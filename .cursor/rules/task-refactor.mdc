<!-- markdownlint-disable -->
---
description: 重构任务规则（Android）
globs:
alwaysApply: false
---

# 重构任务规则（Android）

## 重构目标
- 提升可读性与可维护性
- 降低重复代码
- 简化复杂条件与控制流
- 为后续扩展/测试创造更好的结构

## 重构流程
### 1. 理解现有代码
1. 完整阅读需要重构的代码与周边调用点
2. 明确“当前行为是什么”（成功/失败路径、边界条件）
3. 识别可以拆分的职责与重复模式

### 2. 明确重构范围（避免大而全）
1. 以可回归的最小切片为单位
2. 每次只做一种主要改进（提取/重命名/消除重复/简化条件）

### 3. 重构原则（强约束）
1. **保持功能不变**
- 重构不应该改变外部可见行为
- 若必须改变行为，需要将任务按 `logic_change` 走，并重新评估影响面

2. 小步提交/小步验证
- 每完成一个小步骤就验证一次关键路径（编译/运行/测试）

3. 可读性优先于“聪明”
- 提取方法/类时名称要表达意图
- 避免过度抽象导致理解成本上升

## 检查清单
- [ ] 功能行为与原逻辑一致（关键路径回归通过）
- [ ] 代码结构更清晰（职责划分明确）
- [ ] 重复逻辑已被消除或下沉到合适层级（`core/*` 或公共工具）
- [ ] 条件逻辑已可读（复杂 if 拆解/卫语句等）
- [ ] 异常处理/日志语义保持一致或更完善

## 示例重构模式（写作模板）
- 提取方法：把长方法拆成 `validate*` / `execute*` / `handleError*` 等独立片段
- 消除重复：把相似的流程抽成公共私有函数或公共工具类
- 简化条件：把复杂条件拆分为语义化布尔变量

