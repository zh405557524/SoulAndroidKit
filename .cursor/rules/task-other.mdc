<!-- markdownlint-disable -->
---
description: 其他类型任务规则（Android）
globs:
alwaysApply: false
---

# 其他类型任务规则（Android）

## 通用流程
### 1. 理解需求
1. 复述需求要点（目标/边界/验收标准）
2. 明确需要改哪些能力：UI、逻辑、数据、依赖、权限、资源等
3. 列出未知项并尽量在开始编码前补齐（必要时提问）

### 2. 代码分析
1. 找到关键入口与调用链（Activity/Fragment/测试入口 → 业务实现 → 数据/能力封装）
2. 理解当前实现的“意图”（为什么这么写、有哪些隐藏约束）
3. 评估风险与影响范围：会不会牵连其他模块/测试/资源

### 3. 实现原则
1. 遵循项目已有规范：分层、依赖方向、命名与资源管理
2. 修改尽量小：优先补齐缺失边界条件，而不是大范围重写
3. 异常处理要完善：日志 + 友好失败结果（不要静默失败）
4. 耗时操作要移出主线程：避免 UI 卡顿/ANR

### 4. 验证
1. 关键路径回归：保证“以前能用的仍然能用”
2. 边界条件验证：null/空/权限拒绝/文件不存在/网络失败等
3. 若存在测试页面/测试用例：更新并运行对应的功能测试（见 `02-功能测试.mdc`）

## 检查清单
- [ ] 行为符合需求且不引入回归
- [ ] 代码风格一致（遵循 `04-编码规范.mdc` 与 UI 约束）
- [ ] 依赖方向未被破坏（无循环依赖）
- [ ] 错误处理与日志充分（关键路径可定位）
- [ ] 性能/线程模型正确（耗时不阻塞 UI）

